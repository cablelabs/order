{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "newBillingAccountInformation",
    "id": "newBillingAccountInformation",
    "description": "A billing account information represents contact and address information of a billing account.",
    "type": "object",
    "required": [
        "contact",
        "address"
    ],
    "properties": {
        "contact": {
            "$ref": "contact.json#",
            "description": "Identifies the information of the person or office to be contacted on billing matters."
        },
        "address": {
            "oneOf": [
                {"$ref": "formattedAddress.json#"},
                {"$ref": "fieldedAddress.json#"},
                {"$ref": "geographicPoint.json#"},
                {"$ref": "addressReference.json#"}
            ],
            "description": "Identifies a property address or postal delivery address."
        }
    }
}
